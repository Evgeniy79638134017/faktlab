name = "ilab-form"
main = "worker.js"
compatibility_date = "2026-08-01"

# Секреты не хранятся в этом файле. Ставятся один раз командами:
#   wrangler secret put BOT_TOKEN
#   wrangler secret put CHAT_ID
